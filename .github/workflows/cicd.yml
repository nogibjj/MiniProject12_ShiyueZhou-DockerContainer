name: CI
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Setup Python 3.10
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'
      - name: Set up Environment Variables
        run: |
          echo "SQL_SERVER_HOST=${{ secrets.SQL_SERVER_HOST }}" >> $GITHUB_ENV
          echo "SQL_HTTP_PATH=${{ secrets.SQL_HTTP_PATH }}" >> $GITHUB_ENV
          echo "DATABRICKS_API_KEY=${{ secrets.DATABRICKS_API_KEY }}" >> $GITHUB_ENV
      - name: install packages
        run: make install
      - name: lint
        run: make lint
      - name: test
        run: make test
      - name: format
        run: make format
      - name: deploy
        run: make deploy